<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="android:background">#FFFFFF</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="navigation_menu_item_color" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:activatedBackgroundIndicator">@drawable/navigation_menu_item_color</item>
        <item name="android:selectableItemBackground">@drawable/navigation_menu_item_color</item>
        <item name="android:background">@drawable/navigation_menu_item_color</item>
        <item name="itemTextColor">#FFFFFF</item>

    </style>

    <style name="toolbar_item_color" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="android:textColorSecondary">@color/colorDarkOrange</item>
        <item name="android:background">#FFFFFF</item>
        <item name="android:logo">@drawable/hotpan_toolbar</item>

    </style>

</resources>
